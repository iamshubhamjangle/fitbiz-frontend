import { ExerciseCategoryPipe } from './exercise-category.pipe';

describe('ExerciseCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
